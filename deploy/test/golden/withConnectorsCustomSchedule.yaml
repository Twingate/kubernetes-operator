twingateOperator:
  apiKey: "<api key>"
  network: "<network slug>"
  remoteNetworkId: "<remote network id>"
connectors:
  enabled: true
  count: 4
  schedule: "{{ mul .i 2 }} 0 * * *"
  specExtra:
    podAnnotations:
      some/annotation: "some-value"