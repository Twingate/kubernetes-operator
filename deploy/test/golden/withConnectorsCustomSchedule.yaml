twingateOperator:
  apiKey: "<api key>"
  network: "<network slug>"
  remoteNetworkId: "<remote network id>"
connectors:
  enabled: true
  count: 4
  name: "my-connector-{{ mul .i 2 }}"
  schedule: "{{ mul .i 2 }} 0 * * *"
  specExtra:
    podAnnotations:
      some/annotation: "some-value"