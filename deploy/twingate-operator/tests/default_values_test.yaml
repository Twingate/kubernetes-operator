suite: Snapshot
excludeTemplates:
  - NOTES.txt # NOTES.txt is empty
set:
  twingateOperator:
    network: "<network slug>"
    apiKey: "<api key>"
    remoteNetworkId: "<remote network id>"
tests:
  - it: should render
    asserts:
      - matchSnapshot: {}
